export default function Events() {
	return (
		<section id="events" className="h-50 bg-red-500">
			<h2 className="text-primary dark:text-secondary">Events</h2>
			<p>Check out our upcoming events...</p>
		</section>
	);
}
