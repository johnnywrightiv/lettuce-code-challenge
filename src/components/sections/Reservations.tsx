export default function Reservations() {
	return (
		<section id="reservations" className="">
			<h2 className="text-primary dark:text-secondary">Reservations</h2>
			<p>Make a reservation...</p>
		</section>
	);
}
